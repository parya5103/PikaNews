[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.poetry]
name = "pikanews"
version = "1.0.0"
description = "Production-grade entertainment news aggregator using async scraping and NLP enrichment"
authors = ["Antigravity Developer"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
